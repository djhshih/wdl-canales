task snv_fdr_filter {
	File snv
	File rscript
	String sample_id
	
	Int memory_gb
	Int cpu

	command <<<
		Rscript ${rscript} ${snv} -o ${sample_id}_failed.snv
	>>>

	output {
		File failed_snv = "${sample_id}_failed.snv"
	}

	runtime {
		memory: "${memory_gb} GB"
		cpu: "${cpu}"
	}
}
