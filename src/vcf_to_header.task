task vcf_to_header {
	File vcf

	command <<<
		# extract header lines but remove all columns after INFO
		grep '^#' ${vcf} | sed 's/INFO\t.*/INFO/' > ${vcf}.header
	>>>

	output {
		File header = "${vcf}.header" 
	}
}
